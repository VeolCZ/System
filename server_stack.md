## My stack

### Maintenance

- `unattended-upgrades` for auto server updates

### Docker

- `potrainer` manages all containers running on the server

### Networking

- `ufw` server side firewall
- `tailscale` VPN between all servers
- `fail2ban` Auto throtling on exposed ports
